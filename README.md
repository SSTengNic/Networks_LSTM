This repository serves as a standalone module for the LSTM code used in the STEP Networks project. By separating it from the main codebase, we aim to improve modularity and make the neural network implementation easier to manage and reuse.

Feel free to clone this repository and use the LSTM model for your own training experiments related to congestion control loss ratio prediction.
